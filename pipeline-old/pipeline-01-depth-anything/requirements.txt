# ============================================================
# K9 Crystal Pipeline — Python Dependencies
# ============================================================
# Install: pip install -r requirements.txt
#
# GPU strongly recommended for depth estimation and upscaling.
# CPU fallback is supported but very slow for large images.
#
# Python version: 3.10 or 3.11 recommended
# ============================================================

# --- Core image processing ---
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# --- Upscaling (Real-ESRGAN) ---
# Real-ESRGAN is installed from GitHub, not pip directly.
# See INSTRUCTIONS.md section on setup for the correct install command.
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# --- Background removal ---
# rembg uses ONNX models, no GPU required (but GPU speeds it up)
rembg>=2.0.50

# --- Depth estimation (PyTorch + HuggingFace) ---
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
timm>=0.9.0          # required by some depth models
einops>=0.7.0        # required by Depth Anything V2
huggingface_hub>=0.20.0

# --- Point cloud and mesh processing ---
open3d>=0.18.0
pymeshlab>=2023.12

# --- Facial landmark detection (proof-of-concept, step 03b) ---
# CPU-only, no CUDA needed — used to anchor mesh correction to real
# facial geometry instead of relying on depth-map smoothing alone.
mediapipe>=0.10.14

# --- Utilities ---
python-dotenv>=1.0.0
tqdm>=4.66.0
scipy>=1.11.0
matplotlib>=3.8.0    # for visualizing depth maps during testing

# --- API server (used in pipeline web integration - Project 2) ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
